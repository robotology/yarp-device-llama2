/*
 * SPDX-FileCopyrightText: 2023-2023 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: LGPL-2.1-or-later
 */


// Generated by yarpDeviceParamParserGenerator (2.0)
// This is an automatically generated file. Please do not edit it.
// It will be re-generated if the cmake flag ALLOW_DEVICE_PARAM_PARSER_GERNERATION is ON.

// Generated on: Mon Apr 13 16:55:49 2026


#include "Llama2Device_ParamsParser.h"
#include <yarp/os/LogStream.h>
#include <yarp/os/Value.h>

namespace {
    YARP_LOG_COMPONENT(Llama2DeviceParamsCOMPONENT, "yarp.device.Llama2Device")
}


Llama2Device_ParamsParser::Llama2Device_ParamsParser()
{
}


std::vector<std::string> Llama2Device_ParamsParser::getListOfParams() const
{
    std::vector<std::string> params;
    params.push_back("model_context");
    params.push_back("model_name");
    params.push_back("npredict");
    params.push_back("ngl");
    params.push_back("offload_gpu");
    params.push_back("context");
    params.push_back("prompt_file");
    params.push_back("prompt_context");
    return params;
}


bool Llama2Device_ParamsParser::getParamValue(const std::string& paramName, std::string& paramValue) const
{
    if (paramName =="model_context")
    {
        paramValue = m_model_context;
        return true;
    }
    if (paramName =="model_name")
    {
        paramValue = m_model_name;
        return true;
    }
    if (paramName =="npredict")
    {
        paramValue = std::to_string(m_npredict);
        return true;
    }
    if (paramName =="ngl")
    {
        paramValue = std::to_string(m_ngl);
        return true;
    }
    if (paramName =="offload_gpu")
    {
        if (m_offload_gpu==true) paramValue = "true";
        else paramValue = "false";
        return true;
    }
    if (paramName =="context")
    {
        paramValue = std::to_string(m_context);
        return true;
    }
    if (paramName =="prompt_file")
    {
        paramValue = m_prompt_file;
        return true;
    }
    if (paramName =="prompt_context")
    {
        paramValue = m_prompt_context;
        return true;
    }

    yError() <<"parameter '" << paramName << "' was not found";
    return false;

}


std::string Llama2Device_ParamsParser::getConfiguration() const
{
    //This is a sub-optimal solution.
    //Ideally getConfiguration() should return all parameters but it is currently
    //returning only user provided parameters (excluding default values)
    //This behaviour will be fixed in the near future.
    std::string s_cfg = m_provided_configuration;
    return s_cfg;
}

bool      Llama2Device_ParamsParser::parseParams(const yarp::os::Searchable & config)
{
    //Check for --help option
    if (config.check("help"))
    {
        yCInfo(Llama2DeviceParamsCOMPONENT) << getDocumentationOfDeviceParams();
    }

    m_provided_configuration = config.toString();
    yarp::os::Property prop_check(m_provided_configuration.c_str());
    //Parser of parameter model_context
    {
        if (config.check("model_context"))
        {
            m_model_context = config.find("model_context").asString();
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'model_context' using value:" << m_model_context;
        }
        else
        {
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'model_context' using DEFAULT value:" << m_model_context;
        }
        prop_check.unput("model_context");
    }

    //Parser of parameter model_name
    {
        if (config.check("model_name"))
        {
            m_model_name = config.find("model_name").asString();
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'model_name' using value:" << m_model_name;
        }
        else
        {
            yCError(Llama2DeviceParamsCOMPONENT) << "Mandatory parameter 'model_name' not found!";
            yCError(Llama2DeviceParamsCOMPONENT) << "Description of the parameter: name of Llama2 model";
            return false;
        }
        prop_check.unput("model_name");
    }

    //Parser of parameter npredict
    {
        if (config.check("npredict"))
        {
            m_npredict = config.find("npredict").asInt64();
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'npredict' using value:" << m_npredict;
        }
        else
        {
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'npredict' using DEFAULT value:" << m_npredict;
        }
        prop_check.unput("npredict");
    }

    //Parser of parameter ngl
    {
        if (config.check("ngl"))
        {
            m_ngl = config.find("ngl").asInt64();
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'ngl' using value:" << m_ngl;
        }
        else
        {
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'ngl' using DEFAULT value:" << m_ngl;
        }
        prop_check.unput("ngl");
    }

    //Parser of parameter offload_gpu
    {
        if (config.check("offload_gpu"))
        {
            m_offload_gpu = config.find("offload_gpu").asBool();
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'offload_gpu' using value:" << m_offload_gpu;
        }
        else
        {
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'offload_gpu' using DEFAULT value:" << m_offload_gpu;
        }
        prop_check.unput("offload_gpu");
    }

    //Parser of parameter context
    {
        if (config.check("context"))
        {
            m_context = config.find("context").asInt64();
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'context' using value:" << m_context;
        }
        else
        {
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'context' using DEFAULT value:" << m_context;
        }
        prop_check.unput("context");
    }

    //Parser of parameter prompt_file
    {
        if (config.check("prompt_file"))
        {
            m_prompt_file = config.find("prompt_file").asString();
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'prompt_file' using value:" << m_prompt_file;
        }
        else
        {
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'prompt_file' using DEFAULT value:" << m_prompt_file;
        }
        prop_check.unput("prompt_file");
    }

    //Parser of parameter prompt_context
    {
        if (config.check("prompt_context"))
        {
            m_prompt_context = config.find("prompt_context").asString();
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'prompt_context' using value:" << m_prompt_context;
        }
        else
        {
            yCInfo(Llama2DeviceParamsCOMPONENT) << "Parameter 'prompt_context' using DEFAULT value:" << m_prompt_context;
        }
        prop_check.unput("prompt_context");
    }

    /*
    //This code check if the user set some parameter which are not check by the parser
    //If the parser is set in strict mode, this will generate an error
    if (prop_check.size() > 0)
    {
        bool extra_params_found = false;
        for (auto it=prop_check.begin(); it!=prop_check.end(); it++)
        {
            if (m_parser_is_strict)
            {
                yCError(Llama2DeviceParamsCOMPONENT) << "User asking for parameter: "<<it->name <<" which is unknown to this parser!";
                extra_params_found = true;
            }
            else
            {
                yCWarning(Llama2DeviceParamsCOMPONENT) << "User asking for parameter: "<< it->name <<" which is unknown to this parser!";
            }
        }

       if (m_parser_is_strict && extra_params_found)
       {
           return false;
       }
    }
    */
    return true;
}


std::string      Llama2Device_ParamsParser::getDocumentationOfDeviceParams() const
{
    std::string doc;
    doc = doc + std::string("\n=============================================\n");
    doc = doc + std::string("This is the help for device: Llama2Device\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("This is the list of the parameters accepted by the device:\n");
    doc = doc + std::string("'model_context': context of Llama2 model\n");
    doc = doc + std::string("'model_name': name of Llama2 model\n");
    doc = doc + std::string("'npredict': number of tokens generated by the model\n");
    doc = doc + std::string("'ngl': number of layers to offload to the gpu\n");
    doc = doc + std::string("'offload_gpu': bool value to offload computations to gpu (default = true)\n");
    doc = doc + std::string("'context': context size (use ctx_size = 0 to load context size from the model)\n");
    doc = doc + std::string("'prompt_file': The file containing the prompt for the LLM\n");
    doc = doc + std::string("'prompt_context': The yarp context from which the prompt file should be loaded\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("Here are some examples of invocation command with yarpdev, with all params:\n");
    doc = doc + " yarpdev --device yarp_llama2Device --model_context <optional_value> --model_name <mandatory_value> --npredict 128 --ngl 50 --offload_gpu true --context 4096 --prompt_file <optional_value> --prompt_context <optional_value>\n";
    doc = doc + std::string("Using only mandatory params:\n");
    doc = doc + " yarpdev --device yarp_llama2Device --model_name <mandatory_value>\n";
    doc = doc + std::string("=============================================\n\n");    return doc;
}
